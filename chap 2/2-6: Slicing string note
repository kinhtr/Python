# -*- coding: cp1252 -*-

# Note: starting point is included and ending point is excluded in the slice range 
variable = "desserts"
print("The first 4 characters were:", variable[:4])
print("The last 4 characters were:", variable[-4:])
print("The string backwards was:", variable[::-1])
