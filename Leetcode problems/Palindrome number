class Solution:
    def isPalindrome(self, x: int) -> bool:
        y = 0
        temp = x
        while (temp > 0):
            remainder = temp % 10
            temp //= 10
            y = y * 10 + remainder
            print(y)
        if y == x:
            return True
        else:
            return False
